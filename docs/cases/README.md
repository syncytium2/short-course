# Cases

Incidents from elsewhere in the estate, imported as **teaching specimens**. A fourth
class of document alongside [`../chain/`](../chain/) (how this course was made),
[`../reviews/`](../reviews/) (adversarial reviews of it), and
[`../handouts/`](../handouts/) (what a learner is handed).

A case is **not** a chain node. Nothing here is part of this course's provenance — these
happened in other projects and are filed because they demonstrate something the course
teaches.

⚠ **One file now breaks that rule and says so at the top of itself.**
[`2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md`](2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md)
is native — it happened here, and two of its four points were committed by the session that
wrote it up. **The rule this folder wants is probably "incidents used as teaching specimens,
with provenance stated" rather than "incidents from elsewhere"** — but that is a change to the
folder's charter, so it is flagged here and left for a human rather than edited in.

⚠ **A second file strains the charter on a different axis, added 2026-09-02.**
[`2026-09-02-four-sessions-one-checkout.md`](2026-09-02-four-sessions-one-checkout.md) is
properly imported — it happened in `armory` — so it satisfies the rule the file above breaks.
What it cannot satisfy is the *promise* underneath the rule: **`armory` is private and must
stay private**, so an outside reader cannot open any of its evidence. Every load-bearing claim
is therefore quoted in full rather than cited, and both limits are bannered on the file.

**These are two different questions and should not be settled as one.** *Native vs imported* is
about who wrote it. *Checkable vs unverifiable-by-outsiders* is about whether a reader can audit
it — and that one now has a floor set elsewhere in this repo: material naming a third party does
not enter at all, and this case names none. Landed rather than held, on the judgement that a
quoted-in-full account of a private incident is worth more than the absence of one, and that the
alternative — moving it out — would put the case somewhere with no charter at all. **Still a
human's call, and still flagged rather than edited into the rules above.**

## Two rules, both inherited

**Every case states its own provenance and its own review scope.** Most are written by
the party being evaluated, which is the weakness [`../chain/01-session-record.md`](../chain/01-session-record.md)
carries a banner for — so the same banner applies, plus an appendix separating what
artifacts can settle from what exists only in the retelling. This is
[`../chain/EXCLUDED.md`](../chain/EXCLUDED.md)'s rule applied to imported material:
nothing enters that has not been scoped and read first. Not scanned. Read.

**Every case carries an audience note.** A case can be true, verified, and still wrong
for the room. Explanation cost is a real budget and the course has a fixed amount of it,
so the audience call is recorded on the file rather than re-argued each time someone
finds the case and thinks it is too good to leave out.

## ⚠ The classification this folder does not have — `philosophy`

**Tony, 2026-08-31, asking whether the cases are "clearly marked as philosophy":** *"that's how
i think of it, a class of case."* **They are not marked, and there is no field to mark them
in.** Recorded here rather than fixed, because the taxonomy is his and inventing one would be
the same error as the charter question above.

What a case actually carries today is a **provenance** line (an HTML comment: written-by,
imported-or-native, whether an outside reader can check the evidence) and an **audience** note.
The index's Audience column is doing four jobs at once — reading time, vocabulary needed, which
`points.md` letter it lands under, and review status — and **none of those is a class.** No
frontmatter, no tags, no `class:` field, in any of the sixteen files.

Two things to settle before anything is marked:

1. **What `philosophy` means as a class.** The working guess, unconfirmed: *the payload is a
   claim about knowledge or judgment that survives deleting every mechanism in the case* — the
   incident is the evidence, not the subject. On that reading the candidates are
   [`the-weakest-fix-is-the-most-available`](2026-08-28-the-weakest-fix-is-the-most-available.md),
   [`every-number-was-right`](2026-08-27-every-number-was-right.md),
   [`the-irony-was-the-only-unchecked-claim`](2026-08-30-the-irony-was-the-only-unchecked-claim.md)
   and [`nothing-was-missing-and-it-could-not-be-found`](2026-08-30-nothing-was-missing-and-it-could-not-be-found.md).
   **Nobody has confirmed this list.** A fifth candidate is not in this folder and cannot be —
   see the note below.
2. ~~**Whether it is the same "philosophy" already in this repo.**~~ **ANSWERED by Tony,
   2026-08-31:** *"philosophy section is my own pandoras box. musings to relax the soul."*

   The word is flagged twice here — in [`points.md`](../../points.md) C1 and in the
   [irony case](2026-08-30-the-irony-was-the-only-unchecked-claim.md) — as **"not in this repo
   and has not been imported."** **Those flags are correct and permanent, not a gap anyone
   should close.** The philosophy section is his, it is off-repo by design, and **no session
   should try to import it, index it, or write toward it.** A future session finding those two
   flags will read them as unfinished business; they are not. One already did, on 2026-09-01,
   and wrote *"still unimported"* into the root handoff as an open thread — which is why this
   paragraph is here rather than in a commit message.

   So the class and the room are related but not the same, and the difference is the useful
   part: `philosophy` on a case marks **how it reads** — worth sitting with rather than
   teaching from — and **not where it goes.** Where a case goes is still `points.md`.

### The case this section was written for is not in this folder

The candidate that prompted all of the above — an imported case about a colleague's 209-word
reply that produced ~1,000 lines of verified findings — **is held privately and is not indexed
here.** It quotes a named third party's private correspondence in full, and on 2026-09-01 it
was found pushed to this repository's **public** remote on a stale branch, by omission rather
than by decision. The branch was deleted the same day.

**The rule that came out of it, which is the part worth having:** our own work is public
because a record nobody can open cannot make a claim checkable. That argument does not extend
to other people's words. **Material quoting or naming someone who did not choose to be
published does not enter this repository at all** — not on master, not on a branch — so there
is no promotion step left to forget. It reaches the course only as a de-identified
generalisation, by an explicit call.

The generalisation in this instance needs no email and no name: **correspondence is a test, not
context.** Give an agent the things that are *not* about your code — a reviewer's note, a
colleague's description of their own method — and it reads them as background. They are
specifications. It has **no home in [`points.md`](../../points.md) yet**, and writing one is the
open work.

## Known-wrong content

[`OPEN-CORRECTIONS.md`](OPEN-CORRECTIONS.md) lists statements in **committed** cases that are
wrong, with the replacement. **Read it before teaching from anything here.** A case that reads
well is not evidence its numbers hold, and a correction that lives only in someone's memory of a
conversation is how the wrong figure gets taught twice. One entry is open: **C1**, the cost
figures in Point 5 of the 2026-08-28 case.

## Index

| Case | What it demonstrates | Audience |
|---|---|---|
| [`2026-09-03-the-report-went-to-the-other-agent.md`](2026-09-03-the-report-went-to-the-other-agent.md) | **The orientation existed, was well written, and was addressed to a socket.** In one eight-minute window after Tony typed `go`, a `bugarach` session sent **475 words of status to a peer agent** and **62 words to Tony**, in three lines of 22, 22 and 18 — every one of them *what I am about to do next*, none of them where the work stood. The peer message is a genuinely good report (verified findings separated from taken ones, a refusal with its reason, a self-implicating bug, an explicit refusal to guess a recipient) and **it worked**: it was routed on, and a short-course session landed the repair as `4582603` **three minutes later**. So the machine channel went finding → landed fix in a second repository faster than the human channel managed one sentence. **The failure is not capacity and not writing. It is addressing.** **Point 2:** thirty seconds after arming `merge_when_green.sh` on both PRs — the tool that exists so nobody has to wait — it blocked its own turn on a hand-rolled `time.sleep(30)` poll of the same two PRs, for up to 25 minutes. *An agent that builds an asynchronous mechanism and then synchronously watches it has kept all the cost and none of the benefit.* **Point 3 is the one that should survive:** the same session had written the same diagnosis about itself, to a different peer, fifteen hours earlier — *"the failure is live in this session right now… I never called `SendUserFile` at all"* — **so naming a failure mode accurately, in the same session, changes nothing about the next turn.** ⚠ **Tony's framing is corrected on the file:** he offered it as *"ending without a push"*; the turn had not ended and was still open ten minutes later. He looked during a 62-second gap and could not tell — **which is the case, not a mistake on his part.** Carries the missing sentence written out at 68 words, against the 62 he got. | **Short and late, ~2 min**, no vocabulary beyond *"the tool told another program more than it told you."* Payload is the **missing sentence**, which is a concrete thing a beginner can demand and then check for. **Imported from `bugarach`, which is public** — PR and branch timings are checkable by anyone; the word counts and both quoted messages come from a **local transcript that is not**, so they are quoted in full. Written by a non-participant. Reviewed by nobody. **Deliberately adds no tenth taxonomy category** — nearest is G, but every G instance is project ↔ project with *no channel*, and here the channel worked and skipped the human. Not placed under a `points.md` letter yet |
| [`2026-09-02-four-sessions-one-checkout.md`](2026-09-02-four-sessions-one-checkout.md) | **The pair to the 2026-08-29 native case — two boards used as designed that failed anyway, with the blindness in a different place in each.** ⚠ *This row and the file originally called it "the half that stops 'be disciplined' being the lesson"; that mischaracterised the predecessor, whose own Point 1 says "Nobody was careless" and whose title is "and it was right to be". Corrected on landing; the banner on the file explains why the error was kept on the record.* In 2026-08-29 the **reader** was blind — a session amended its board block to name the file it was editing and `claim.sh --list` skipped it for being marked `DONE`. Here the **schema** is blind: it records claims, and the collision was about ownership. **The file gained a fifth finding in review, and it is the best one: the reviewer checking finding 4 ran `git ls-files` inside each repo, got four-of-four tracked, and had a false correction half-written — because the five unversioned boards sit at the top level of the `*-worktrees` directories, which are not repositories.** A correct method applied to the adjacent question, returning a clean result, about to be published as a verdict on someone else's work. **Caught by arithmetic, not reasoning** — five-and-550KB did not match four-and-364KB — which is the rule it leaves behind: a claim stated as a count can be audited, a claim stated as a tendency cannot. Four sessions in one working copy of a private sibling repo built a claim board after colliding, and it worked — it caught the next collision the same afternoon. Then it missed one, **because the board records claims and the collision was about ownership**: a file nobody had claimed already had an author, ownership was inferred from whoever was *discussing* the bug, and the same fix landed twice, twenty minutes apart, in two worktrees off one commit. Both merged clean; git reported nothing, because two good fixes to one bug are not a conflict. **A Touches column answers *is anyone claiming this* and never *who owns this*, and one shared git identity across four sessions means `git log --format=%an` cannot supply the difference.** Two more: **(2)** a selftest's hard case had already been made hermetic *with a comment above it stating the principle*, and the four cheap cases above that were left reading the real repo — so the file carried a correct statement of the rule directly above four cases breaking it, and **a half-fix is harder to see than no fix because the comment reads as evidence.** It surfaced only because a stranger's uncommitted file in a sibling working copy made the gate fire correctly and the assertion go red with nothing broken — **green on the runner, red on the laptop.** **(3)** a session told *"commit and push as needed"* found **90 dirty entries** of another session's mid-run output and was one tool call from committing it under a message describing none of it; it stopped on **file timestamps read on a hunch**, which is not a control — and the board's first draft credited its own rule for the save until the near-missing session asked for the correction. **The through-line: the fix for the visible failure moved contention somewhere the tools cannot look.** Separate working copies stop sessions overwriting each other and stop anything noticing when they duplicate each other instead. | **Second example, never the opener** — needs *a check that cannot fail* already established by a single-session case, then it lands hard. **Findings 2 and 3 need no vocabulary**; finding 1 needs the reader to accept several assistants in one folder as a premise, which is not most scientists' situation yet. ~3 min for 2 and 3, ~8 for 1. **Do not teach finding 1 as a coordination-tools problem** — it is a naming problem, claims versus ownership. Pairs with `2026-08-29-the-board-was-empty`: that one is a board nobody wrote on, this one a board written on correctly that could not see the collision anyway, and a reader who takes only the first concludes the answer is discipline. **Written by a participant, same day, unreviewed, and the author committed finding 1** — appendix separates what the artifacts settle from the retelling. **Evidence is in a private repo an outside reader cannot open**, so everything load-bearing is quoted rather than cited. Not proposed for the website |
| [`2026-09-02-thirty-two-minutes-and-four-sentences.md`](2026-09-02-thirty-two-minutes-and-four-sentences.md) | **The counterpart to the case below, same day, and the strongest provenance in this folder.** Idea to a working local app in **32m 30s** — first typed word to the commit carrying search, workout mode and editing — from **four typed sentences**, two of which are administrative. What existed at the end: 1,058 workouts imported from ~1,130 `.docx`/`.xlsx` files spanning 2011–2026 (67 groups merged as re-teachings rather than dropped), four routes including a timer built on the source data's own interval notation, a clean `tsc` and a clean `next build` (both re-run the next day), and a decisions file **stating its own unfinished edges with counts**. **Point 1:** the specification was one sentence, and the other three messages are an address, a name and three constraints — **no correction, no re-scoping, nobody ever restating what they had already said.** That is what `cold-start` W2's *"say the whole thing in one sentence"* is actually buying, and the step has always been hard to justify because it costs nothing. **Point 2 is the payload and it is deliberately not about vendors:** set beside the form-builder walk — same day, one person, one evening, one idea — the difference is not quality and not the model. The office assistant did its job correctly and helped a person make a document. What separates the rows is **which side of the keyboard the making happens on**, which is a property of the *kind* of tool and is checkable in sixty seconds — the reason `cold-start`'s repair is a test rather than a list of product names. **Point 3:** two live defects found by *running* the repo rather than reading it — a README saying *"Nothing imported yet"* fifteen minutes after the import commit, and two correct totals (1,130 files, 1,058 workouts) in two files with no unit on either. ⚠ **Carries an explicit *what this does not show*: not a beginner, not a controlled comparison, scaffolding already his, and a clean build is not a finished product.** | **Beginner headline ~1 min** — four sentences, 32 minutes, and a working thing; the numbers do the work with no vocabulary at all. **Use it only as a pair with the case below** — alone it reads as an advertisement, and the *what this does not show* section is not optional trimming. **The only artifact-backed timing measurement in this repo**, and the natural evidence for W2 and for the browser route's premise that the agent works the files directly. Written by a **non-participant, entirely from a git history, an archived transcript and two builds**; every number carries its command in an appendix. **One figure corrected in the useful direction** — reported as 37 minutes, measured at 32m30s. Reviewed by nobody. An email address in the transcript is redacted; this repository is public |
| [`2026-09-02-fifteen-screenshots-into-a-form-builder.md`](2026-09-02-fifteen-screenshots-into-a-form-builder.md) | **The first walk of our own material by somebody outside this project, and it did not finish.** A beginner took [`cold-start.html`](../handouts/cold-start.html)'s browser route with the assistant her institution gives her — a **Copilot inside a Microsoft app suite** — and it did exactly what it is built to do: **opened a low-code form builder and had her place list boxes and type field names by hand**. Most of an hour on linking a cloud folder; no repository at the end; the session saved by **pasting it into a note**. **Point 1:** the masthead sentence admitting the page is written for one vendor carried `data-tiers="mid max"` — so the route with the least equipped reader was the only route shown no vendor caveat, while being **the most vendor-specific of the three**, naming one company's website end to end. It was tagged that way deliberately, by a fix that was right about the macOS half and removed the wrong caveat. **Point 2 is the payload and is not about vendors: the remedy is what carried her further in.** Every phase of the page ends in *screenshot it and show your agent*, backed by a whole companion handout and by real evidence — five wrong button names in one afternoon, three unstuck in a single exchange. She used it about **fifteen times and it worked perfectly every time**, because a good assistant asked *what now* always has an answer, and **every correct answer walked her one screen deeper into the wrong product.** A remedy with no way to detect that it is the wrong remedy does not fail, it **accelerates**; nothing on the page says when to stop asking and leave. **Point 3:** the page opens by promising *"a session that leaves a record behind it"* and **no step on that route ever checked the record existed** — W3 proved the agent could read the repository and nothing proved it could write. **Point 4:** `tier_check.sh`, built the day before for exactly this page's route defects, was green and is not broken — it proves reachability, not that the prose above the boxes is true for the reader shown it; its standing warning names the **mirror image** arrangement (route-aware prose over route-blind boxes) and is silent on this one (route-blind prose over route-split boxes). Repaired the same hour in `1f42e0f` / `71323d4`: a product-neutral gate above W1 whose test is *ask it for a file and see whether you get a file*, a W1 box for it, and write-proofs at W3 and W4. | **Beginner headline ~2 min, no vocabulary at all** — the florist allegory carries Points 1 and 2 (directions that say *go in and ask*, a shop where everyone is helpful and correct, fifteen questions and a very good bouquet and no key). **The most important case in this folder for the course's own claim**, because it is the only one where the material we teach *from* was walked by the audience we teach *to*, and the thing that hurt her was our recommended remedy working. **Bears directly on `show-it-your-screen.html`, which currently states no limit at all.** ⚠ **Weakest provenance here and it says so at the top: no transcript, no screenshots, no repository — four sentences relayed twice, as it happened.** The mechanism is checkable in this tree; the fifteen, the hour and the note are **retelling and must not be quoted as measurements**. Written by the session that made the repair; **no review of any kind, same-hour turnaround** |
| [`2026-09-01-the-number-agreed-for-an-unrelated-reason.md`](2026-09-01-the-number-agreed-for-an-unrelated-reason.md) | **The first import in a fortnight, and the first outside instances of this repo's most-replicated native finding.** Four incidents from one working day on another project's analysis pipeline, one shared mechanism: **a number matched for a reason unrelated to the question being asked of it, so the check meant to catch the problem passed.** (1) A replay validated by *"0 bound violations"* — where the bound tested one direction and **three of four code differences pushed that way**, because the replay was written against a **second vendored copy** of the algorithm and every citation in its documentation resolved only in the tree that had not run. (2) A collector raised its own alarm — *195 result rows but 192 files* — and the discrepancy was **explained correctly and left in place**: three jobs that built a full result and lost it finished with an empty error string, so **a total wipeout was a fourth category the classifier did not have**, and the obvious repair reports them as crashes. (3) A mangled status banner offered as proof of a file-corrupting defect; measured at byte level it is an **encode** defect that never reaches a file, while the corrupting one is a **decode** defect on a different line — **a fix validated against the banner would have looked successful.** (4) A selftest failing `(11 vs 12)` beside a review process defining exactly 11 roles, read as *upstream added a role*; the numbers count **version stamps in a shell script** and the 11 was a coincidence — relayed onward and stated ***flat***, which the case names as *the moment verification stopped, not the moment it became certain*. **Points 1 and 2 are defects in a document that had already passed a formal eleven-role adversarial review** — the review was real and never asked whether the citations resolved in the code that ran. **Point 5 is the payload and is not on the published source:** publishing was refused because the file contained a literal U+FFFD pasted from case 3's own banner — **a guard at a layer the author did not control refused a document about character corruption for containing character corruption.** The only instance where the check fired correctly and caught the author, and **the difference is only where the check sat**: at the write, not downstream of the error. | **Advanced — and the reason is the useful part: the mechanisms are each a paragraph, but they only land on someone who has already shipped a green check they later distrusted.** A beginner reads *be careful*; the intended reading is *your existing checks have this shape and you cannot see it from inside.* ~5 min setup for points 3 and 4, ~10–12 for 1 and 2 (one concept each: a vendored duplicate library, and a batch classifier deciding which outcomes are owed a file). **Point 1 is the expensive one** — budget extra or it reads as a filing error rather than a verification failure. Point 5 is free and is the natural closer. No MATLAB, imaging, or scheduler vocabulary needed; **science trimmed deliberately, keep it trimmed.** **Gives us a term we lacked — *stated flat*** — and raises something our material does not cover: **an alarm that fired correctly and was closed by explanation.** Point 5 argues **position, not strength, is what made a check work**, which the four-tier table does not currently say. **Written by a participant** — self-report on points 3 and 4, and the fuller, less flattering authorship account was volunteered when the folder's charter asked for it. **Mechanisms verified with commands; the *"0 bound violations"* result and the 195/69/3 split are retellings and are marked as such in place.** Not murderboarded — **the review process was itself offline, stale because the tool that updates it carries the point-3 defect.** Not proposed for the website |
| [`2026-09-01-the-index-worked-and-the-trap-was-not-a-question.md`](2026-09-01-the-index-worked-and-the-trap-was-not-a-question.md) | **The sequel to the case below, and the remedy is the subject.** The keyword index that case ends with shipped 2026-08-31 as `81cc134`. The next day a session **used it successfully twice in one hour** — one hit, one honest miss repaired on the spot — and then paid a trap that was **the first row of a table in that same file, shipped by that same commit, one day old**, thirty lines from a row it had just used. It then wrote the trap into a pull request **as a new discovery**, re-deriving the row's own phrase `fails toward green`, and was corrected only because the next instruction it got happened to ask about traps. The trap itself is a clean **fails-toward-green** specimen: a worktree's `pytest` imports the primary checkout's `src`, so the run is this branch's tests against `main`'s code — **2 failed / 1672 passed** uncorrected against **1674 passed** with `PYTHONPATH`, where the danger is not the two failures but the 1,672 meaningless passes and a summary line identical in tone either way. **The payload is Point 4, a proposed limit on N2/B4's remedy clause:** *put it where they already look* was satisfied completely and did not work, because **the unit of retrieval is the query, not the file** — a reader navigates to the section their question names, which is what an index is *for* — so **an index can only reach someone who already suspects they need it, and a trap is by definition what you need before you know to ask.** Not the index failing (Point 1 says it worked) and not the wording (its keywords were the right ones and every one would have matched, had the session had any reason to type them). Also **Point 5, optional — four self-inflicted errors in four hours, and what they have in common.** The register built in response defaulted its estate scan to `dirname(repo)` and from a worktree **scanned one repo and called it the estate** — the registered trap wearing a different hat, committed by the session that had just written it up. A stale propagation figure came two files from being propagated by a document arguing that written-down knowledge goes stale. **The backlog count shipped wrong and then its fix shipped wrong** — first counting `nvm` (nvm-sh/nvm, vendored on the machine) as an estate repo, then reclassifying **`interface2`**, the repo bugarach vendors its session protocol *from*, as third-party, because the estate is not one forge. And the register **counted the document about the register**, leading with *"2 of 2 traps have nothing that stops them"* in the commit that added that document. **The payload of Point 5 is the direction:** three of the four made the headline number *worse* — more repos, more unstopped traps — and survived because **a self-critical number does not feel like it needs checking.** | **Beginner headline ~2 min, body ~10 min.** The **extractor-fan allegory** carries Points 1–4 with **no software vocabulary at all** (a list of known hazards pinned beside the lookup cards someone read twice, and they gas themselves anyway — because you look things up when you have a question, and they did not have one). Point 2 is the folder's strongest fails-toward-green specimen: the misleading summary is quoted beside the true one. **N5 instance 8, and the first where instance 7's remedy was in place and working at the moment of failure** — bears directly on **N5 decision 3**. **Pairs with the case below as a ~15 min two-parter** — build the remedy, then follow it to its limit rather than to its merge; the only place in the folder that does. **Point 5 is advanced and reads as piling on — cut it unless the room is already asking whether knowing about a failure helps.** Written by the party being evaluated; **not murderboarded**, artifact verification only, one self-correction marked in the banner |
| 🚧 [`2026-08-30-nothing-was-missing-and-it-could-not-be-found.md`](2026-08-30-nothing-was-missing-and-it-could-not-be-found.md) | **DRAFT — the upstream artifact is an open PR still being revised.** A session spent several turns re-deriving that another lab's data-import machinery existed, and began designing around a constraint the importer in its own tree had already solved. **Nothing was missing and nothing was disorganised** — 148 todos, 116 artifacts, 90 tools, all filed correctly, none of it addressable by anyone who did not already know it was there. **Seventh instance of [N5](../../OPEN-FINDINGS.md)'s retrieval diagnosis and the first where the thing not retrieved was not a rule**, which puts it outside B4's jurisdiction: you cannot make an importer fire. The remedy built for it — a keyword index, keyed on the words that are *not* in the path — then shipped **its own dead row, pointing at a branch that had not landed** — one worker citing their own unlanded work of three hours earlier, so **not a coordination failure a board would catch** — unseen by its guard because the guard resolves markdown links and the known-traps section is written in code spans. **It was mutation-tested, in the region where it worked** — and found by review, not by the check. Repaired same-day with the line worth stealing (*a row may only point at something that exists on `main` — land it, then index it*). **Then Point 7, which is the payload: the 224-byte paragraph announcing the index pushed the session briefing 80B over its budget, degrading it — and what the degraded form drops is the standing correction `TTX IS NOT A SILENCING CONTROL`,** which exists because a session once moved to calibrate the detectors until the data agreed with the textbook. **The announcement broke the channel that carries it, and the line it cost is the one saying do not substitute the paradigm for the measurement — the same failure as Point 1, one layer down.** Red for a day while four PRs merged past it, then landed by **raising the budget rather than trimming to fit it**, and by cutting the announcement to `docs/INDEX.md FIRST — keywords.` — **the address survived, the origin story went.** | **Beginner headline ~2 min** (a labelled shelf, organised by what things *are*, asked in terms of what things are *for* — no vocabulary at all), **body ~10 min.** Needs one idea installed: an assistant sees only what it has loaded, and a real project does not fit — **which no point in `points.md` currently states directly.** Bears on **N5 decision 3** (does *retrieval at the point of action* earn its own point) and supplies the second-repo instance **N2** named as what would settle it; proposes that an artifact's tier is set by whether its failure is **detectable**, not by what it is made of. Imported from `bugarach`, **but written by the session that found the defect it reports** — not the disinterested import this folder's charter imagines |
| [`2026-08-30-the-irony-was-the-only-unchecked-claim.md`](2026-08-30-the-irony-was-the-only-unchecked-claim.md) | **An agent said the change had never landed, in the same message where it correctly said the change would land by itself. It landed 4m26s later.** Four checkable specifics in that message — the timestamp, the merge method, the watch, the mechanism — were all correct. **The only unchecked statement was the self-deprecating aside, and it was false twice:** the PR merged on time, and the file it was said to have settled is not in its diff (`docs/handoffs/…` vs the root `HANDOFF.md` — two channels sharing a word). The half that made it convincing is **true**: the root handoff really is stale, documented and bannered that morning — so the defect is not a false fact but a **false cause**, where both halves check out separately. Also: what landed is **not what was armed** — a squash auto-merge was enabled and never disabled, and `main` carries a two-parent merge commit. | **✅ ACCEPTED INTO THE COURSE 2026-08-30** — *"yes add it. we can pare down as needed."* **Beginner — ~2 min, ~40 seconds of setup** (a change waiting for approval; approval can happen by itself). No domain vocabulary at all; the subject of the change is irrelevant. **The only case here about a *register* rather than a number, a gate, or a file** — asks *what in a report even looks like a claim?*, and notes that self-criticism reads as rigor, which is the register this whole folder is written in. **Landed in [`points.md`](../../points.md) under C1**, where it inverts C1's own point (the report was in plain English and the plain English is what was wrong), plus a G5c pointer for PR-as-handoff. The argument against — fifth self-report case, third from one day — was overruled, not answered, and is kept on the file. Written by a **non-participant**, entirely from the GitHub PR record; **the other session was never asked**, and whether its own watch later corrected the record is unsettled. Role-1 claim check, **NOT murderboarded** |
| [`2026-08-30-the-hedge-that-crossed-a-session-boundary.md`](2026-08-30-the-hedge-that-crossed-a-session-boundary.md) | **The user wrote *"maybe"*. The agent wrote *"Tony's call"* — to a second agent, editing the page the decision governs.** The input carried three signals it was a proposal (a hedge, a literal `[?]`, an incomplete list) and all three were read past. The `UserPromptSubmit` hook built for exactly this two days earlier **could not fire: it watches short replies, and this ambiguity arrived inside a long technical message.** Also: what degrades under relay is not the content but the **modality** — *maybe* → *is being* → *Tony's call*, and no step was a lie; the agent had **read the rule that same session** and recited it back unprompted; and the linter reserves an ID for one unbuilt rule while the rule this incident needed has no ID at all. Cost was one retraction — **filed for the interval, not the damage.** | **Beginner-legible — ~2 sentences of setup** (that several agents work on one project and message each other). No domain vocabulary; the science is scenery and can be replaced with "a technical proposal" throughout. **The only case covering relay between agents**, and the only one where the failure was diagnosed, specified and queued *before* it recurred. Second B4 instance where prose was **actively held in mind** and still lost. Written by the party being evaluated; **role-1 claim check only, NOT murderboarded** |
| [`2026-08-27-the-claim-that-gained-a-source.md`](2026-08-27-the-claim-that-gained-a-source.md) | An eleven-role review examined a false claim, flagged it correctly, and shipped it. A claim can *gain* provenance passing through an agent; a partial flag reads as a receipt; a defect can walk between two roles that both did their jobs. | **Advanced — standalone.** ~30 min of scaffolding. Not for the beginner course (decided 2026-08-27) |
| [`2026-08-27-computed-instead-of-asking.md`](2026-08-27-computed-instead-of-asking.md) | **Two incidents, twelve hours apart, no shared evidence.** An agent that cannot resolve an address computes something instead of stopping — once where no canonical address existed, once where the contract was correct and current and nothing *declared* which folder. Also: a guard whose channel the incident never travelled through. | **Beginner-legible.** ~1 paragraph of setup. Merged 2026-08-28 from two independent accounts; the 11-role review covers incident A only |
| [`2026-08-28-six-prose-rules-zero-mechanized-rules.md`](2026-08-28-six-prose-rules-zero-mechanized-rules.md) | **In one session, six written-down rules were broken and no mechanized rule was.** A 700-line instruction file, read in full at startup, diagnosed the problem in its own text and could do nothing about itself. Central artifact: a commit whose message asserts the opposite of its diff, and the correction that follows it. Also: an agent routing *around* a gate with a different tool, producing exactly the corruption the gate prevents. | **Beginner-legible.** ~1 paragraph of setup (what a hook is). **Not reviewed** — one session's self-report, filed for the commit pair |
| [`2026-08-28-the-weakest-fix-is-the-most-available.md`](2026-08-28-the-weakest-fix-is-the-most-available.md) | Hours after helping write a case about B4, the agent proposed preventing a recurrence by adding a rule to `CLAUDE.md` — the mechanism B4 exists to say does not work. In the same session, prose lost to a hook three times, and `CLAUDE.md` had been factually wrong about its own repo for a week. Carries the **four-tier enforcement table** (prose → checklist → test → structure) and the user's mantra that beat the agent's fix: *never change the data at source*. | **Beginner — ~5 min for the table.** No prerequisites. **Strongest B4 instance we have**; also A2 and B7. Recommended as the B4 opener. Pairs with the case below (proposed 2026-08-28) |
| [`2026-08-28-the-tests-were-defending-the-bug.md`](2026-08-28-the-tests-were-defending-the-bug.md) | A safety tool built to catch checks that cannot fail shipped as one. Two of its tests had been green since the day it was written and were describing a file-deleting bug as correct behaviour — so the fix made them go **red**, and a careful maintainer would have restored the bug. Also: a ban naming three tools while granting a shell that contains all three, and a run record that failed its own gate by quoting itself. Rollout was paused before anything shipped. | **Beginner headline (~2 min), advanced body (~20 min setup).** Candidate for B4 and B2; carries clean A3 and B7 instances. **Proposes a correction to B4's wording** — your own tools inherit the same distrust. Written by the party being evaluated (proposed 2026-08-28). ⚠ **Point 5's cost figures are wrong — [OPEN-CORRECTIONS C1](OPEN-CORRECTIONS.md#c1--2026-08-28-the-tests-were-defending-the-bugmd-point-5--the-cost-figures-are-low-by-19), unapplied** |
| [`2026-08-27-every-number-was-right.md`](2026-08-27-every-number-was-right.md) | 63 of 63 numbers verified correct, every gate green, a kernel reimplementation agreeing to 8e-9 — and an eleven-role review returned 31 blocking findings, none of them arithmetic. The anti-drift machinery worked and prevented nothing that mattered. Also: the agent walked into a retraction it had read that session, three roles contradicted each other usefully, and the human found what the reviewers structurally could not — that it was a page about the wrong thing. | **Beginner headline (~2 min), advanced body (~15 min setup).** Candidate for B2 and A3; carries clean B4 and C1 instances. Written by the party being evaluated (proposed 2026-08-27) |
| [`2026-08-28-the-skip-was-the-whole-story.md`](2026-08-28-the-skip-was-the-whole-story.md) | A library was declared and never installed, so eleven checks on a published result stood down for ten days — reported as `1 skipped`, exit 0, badge green. Switching the alarm on found, inside one CI run, that the published numbers reproduced only on the machine that made them. Then the first repair was wrong about the cause (*"I repeated that error by believing it"*), and a fairness assertion was found passing because it compared **seeds** instead of **recordings**. | **Beginner headline (~2 min), advanced body (~15 min setup).** **Closes the `points.md` B4 worked example**, whose prediction about the fix was wrong in the useful direction. Candidate for B4 and B2; carries clean A3 and B6 instances. Written by a session that was not there; verification appendix upgraded three rows and turned up one finding no commit reports (proposed 2026-08-28) |
| [`2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md`](2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md) | **The first native case here — it happened in this repo, on 2026-08-29.** A cross-session board built the previous day, working and selftest-green, was **blank at the moment two live sessions collided in the same files** — because the session that should have been on it released its claim and then worked for 37 more minutes. It *did* record what it was writing, into a `DONE` block's `Writes:` line, which `--list` skips: replayed, the tool prints `(no active claims)` against a board that names the file. Also: `git` cannot attribute a commit to a session, and **the investigating session grepped its own live transcript, counted mentions its own investigation had just written, and pushed a false confession to every machine.** | **Beginner headline (~2 min), short body (~10 min).** Point 4 needs zero vocabulary. **Third C3 instance and the first about two sessions *simultaneously live*** rather than about session end. Carries B4 (a tier-2 mechanism dressed as tier-4, beside a real tier-4 hook in the same `.claude/`), B7 rule 3 turning on itself, and C1 from the inside. Points 1–3 written by a non-participant; **Point 4 by the party being evaluated** |
| [`2026-08-29-the-third-attempt-introduced-the-defect.md`](2026-08-29-the-third-attempt-introduced-the-defect.md) | **The assistants were not ignoring the documentation — they were obeying it.** Twelve words in a module docstring (*"the interval between them is its duration"*) licensed a computation the project spent a month saying was not its to make, and spread to fourteen files including the public site. **The clause was added by the commit whose stated purpose was to stop that file misleading people** — *"The third attempt at a sentence two people have now been misled by"* — and corrected nothing. Also: an open todo's recommendation kept being executed; the prose was the live defect while the code defect never shipped a number; and the new prose rule already misses two strings on the deployed page. | **Beginner headline (~2 min), advanced body (~10 min setup).** **Rule 10's cost side**, which `points.md` currently states only as an upside — and a rare *counter*-instance to B4, where prose was the most effective enforcement in the project and was pointed the wrong way. Carries B7 rule 3 (a stale todo is a standing instruction), a discriminator B7 rule 8 needs (jurisdictional vs numerical bans), and a **fourth selftest instance** one day old. **Carries a two-channel allegory (Tony's) that teaches Points 1–3 with no software vocabulary** — a 12:15 email nobody opened, against a laminated card on the instrument everybody read and obeyed. **A session has no inbox:** every conversation reaches one reader and does not survive them, so the files win. Written by a non-participant; two edits in the branch are marked as mine |
| [`2026-08-29-two-sessions-three-minutes-apart.md`](2026-08-29-two-sessions-three-minutes-apart.md) | **Second native case.** Two sessions spawned the same eleven-role review of the same page **2m51s apart**. One had posted *"are you in this file?"* on the shared board five minutes earlier and read the silence as an answer; the other's *"STOP"* arrived eight minutes after twenty-two agents were already in flight. **The board was not empty — it was staffed, accurate, and slower than the decision.** The STOP also dated the other run *"since ~15:55"* when its own transcripts start at **16:12:30**, and the author of this case repeated that figure into a handoff unchecked — the third time in one afternoon he passed on a confident number. Also: the accidental duplicate is an experiment nobody would fund, and **the two blind panels converged on the same top three defects.** | **Beginner headline (~2 min), short body (~10 min).** **Fourth C3 instance and the first where the mechanism was working correctly at the moment it failed.** Strongest B4 in the repo — nobody skipped the mechanism; the failure was underneath it. Also C1 and B2/A3. **Written by one of the two parties, who never asked the other**; the hinge fact is read in the other's favour |
| [`2026-08-30-the-gate-blocked-its-own-installation.md`](2026-08-30-the-gate-blocked-its-own-installation.md) | **Third native case.** The one repo in the estate without the heredoc gate was **this one** — the course that teaches that gate as its worked example. Fitting it found the gate would have installed **unable to refuse**: turnstile downgrades any hook lacking a `gate` declaration to advisory, and **the vendor's own three verification commands cannot detect it, because they test the hook directly rather than through the wrapper it is registered behind** — the same shape as the 2026-08-18 fail-open that shipped to seven repos. Then the gate refused its own installation instructions, its own commit message, and the replay of its own near-miss, each fixed in under a minute by moving content one layer off the command line — which is exactly how the corruption it prevents was once produced. Also: there is **no propagation mechanism at all**, so the human step was never skipped because it never existed; and the finding this produced said *"the estate"* having audited one laptop. **Then Point 6: while committing this very file, the author ran the repo's pointer checker (`every pointer resolves`), did a whole-file `git add` on a README another session was editing, and pushed two commits carrying a link to a file that was not in the tree — because the checker scans the working tree and what shipped was a commit. The same failure as Point 1, in the author's own hands, ninety seconds after writing Point 1 up.** | **Beginner headline (~2 min), body ~10 min setup** (what a hook and a wrapper are). **Point 4 is the payload and needs no vocabulary: a harness instruction re-delivered every turn told the agent to use heredocs while `CLAUDE.md` said not to — B4's diagnosis is wrong, prose is not losing to whim but to better-positioned prose.** **Points 1 + 6 as a pair (~4 min) are the most teachable thing here** — knowing a failure by name does not put it in the hands that run the next command. Proposes a B4 refinement, a second incident for §8, and N4. Written by the party being evaluated; **Point 5 was found by Tony and Point 6 by the session it collided with — neither by the author** |
