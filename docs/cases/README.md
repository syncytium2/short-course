# Cases

Incidents from elsewhere in the estate, imported as **teaching specimens**. A fourth
class of document alongside [`../chain/`](../chain/) (how this course was made),
[`../reviews/`](../reviews/) (adversarial reviews of it), and
[`../handouts/`](../handouts/) (what a learner is handed).

A case is **not** a chain node. Nothing here is part of this course's provenance — these
happened in other projects and are filed because they demonstrate something the course
teaches.

⚠ **One file now breaks that rule and says so at the top of itself.**
[`2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md`](2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md)
is native — it happened here, and two of its four points were committed by the session that
wrote it up. **The rule this folder wants is probably "incidents used as teaching specimens,
with provenance stated" rather than "incidents from elsewhere"** — but that is a change to the
folder's charter, so it is flagged here and left for a human rather than edited in.

## Two rules, both inherited

**Every case states its own provenance and its own review scope.** Most are written by
the party being evaluated, which is the weakness [`../chain/01-session-record.md`](../chain/01-session-record.md)
carries a banner for — so the same banner applies, plus an appendix separating what
artifacts can settle from what exists only in the retelling. This is
[`../chain/EXCLUDED.md`](../chain/EXCLUDED.md)'s rule applied to imported material:
nothing enters that has not been scoped and read first. Not scanned. Read.

**Every case carries an audience note.** A case can be true, verified, and still wrong
for the room. Explanation cost is a real budget and the course has a fixed amount of it,
so the audience call is recorded on the file rather than re-argued each time someone
finds the case and thinks it is too good to leave out.

## Known-wrong content

[`OPEN-CORRECTIONS.md`](OPEN-CORRECTIONS.md) lists statements in **committed** cases that are
wrong, with the replacement. **Read it before teaching from anything here.** A case that reads
well is not evidence its numbers hold, and a correction that lives only in someone's memory of a
conversation is how the wrong figure gets taught twice. One entry is open: **C1**, the cost
figures in Point 5 of the 2026-08-28 case.

## Index

| Case | What it demonstrates | Audience |
|---|---|---|
| [`2026-08-27-the-claim-that-gained-a-source.md`](2026-08-27-the-claim-that-gained-a-source.md) | An eleven-role review examined a false claim, flagged it correctly, and shipped it. A claim can *gain* provenance passing through an agent; a partial flag reads as a receipt; a defect can walk between two roles that both did their jobs. | **Advanced — standalone.** ~30 min of scaffolding. Not for the beginner course (decided 2026-08-27) |
| [`2026-08-27-computed-instead-of-asking.md`](2026-08-27-computed-instead-of-asking.md) | **Two incidents, twelve hours apart, no shared evidence.** An agent that cannot resolve an address computes something instead of stopping — once where no canonical address existed, once where the contract was correct and current and nothing *declared* which folder. Also: a guard whose channel the incident never travelled through. | **Beginner-legible.** ~1 paragraph of setup. Merged 2026-08-28 from two independent accounts; the 11-role review covers incident A only |
| [`2026-08-28-six-prose-rules-zero-mechanized-rules.md`](2026-08-28-six-prose-rules-zero-mechanized-rules.md) | **In one session, six written-down rules were broken and no mechanized rule was.** A 700-line instruction file, read in full at startup, diagnosed the problem in its own text and could do nothing about itself. Central artifact: a commit whose message asserts the opposite of its diff, and the correction that follows it. Also: an agent routing *around* a gate with a different tool, producing exactly the corruption the gate prevents. | **Beginner-legible.** ~1 paragraph of setup (what a hook is). **Not reviewed** — one session's self-report, filed for the commit pair |
| [`2026-08-28-the-weakest-fix-is-the-most-available.md`](2026-08-28-the-weakest-fix-is-the-most-available.md) | Hours after helping write a case about B4, the agent proposed preventing a recurrence by adding a rule to `CLAUDE.md` — the mechanism B4 exists to say does not work. In the same session, prose lost to a hook three times, and `CLAUDE.md` had been factually wrong about its own repo for a week. Carries the **four-tier enforcement table** (prose → checklist → test → structure) and the user's mantra that beat the agent's fix: *never change the data at source*. | **Beginner — ~5 min for the table.** No prerequisites. **Strongest B4 instance we have**; also A2 and B7. Recommended as the B4 opener. Pairs with the case below (proposed 2026-08-28) |
| [`2026-08-28-the-tests-were-defending-the-bug.md`](2026-08-28-the-tests-were-defending-the-bug.md) | A safety tool built to catch checks that cannot fail shipped as one. Two of its tests had been green since the day it was written and were describing a file-deleting bug as correct behaviour — so the fix made them go **red**, and a careful maintainer would have restored the bug. Also: a ban naming three tools while granting a shell that contains all three, and a run record that failed its own gate by quoting itself. Rollout was paused before anything shipped. | **Beginner headline (~2 min), advanced body (~20 min setup).** Candidate for B4 and B2; carries clean A3 and B7 instances. **Proposes a correction to B4's wording** — your own tools inherit the same distrust. Written by the party being evaluated (proposed 2026-08-28). ⚠ **Point 5's cost figures are wrong — [OPEN-CORRECTIONS C1](OPEN-CORRECTIONS.md#c1--2026-08-28-the-tests-were-defending-the-bugmd-point-5--the-cost-figures-are-low-by-19), unapplied** |
| [`2026-08-27-every-number-was-right.md`](2026-08-27-every-number-was-right.md) | 63 of 63 numbers verified correct, every gate green, a kernel reimplementation agreeing to 8e-9 — and an eleven-role review returned 31 blocking findings, none of them arithmetic. The anti-drift machinery worked and prevented nothing that mattered. Also: the agent walked into a retraction it had read that session, three roles contradicted each other usefully, and the human found what the reviewers structurally could not — that it was a page about the wrong thing. | **Beginner headline (~2 min), advanced body (~15 min setup).** Candidate for B2 and A3; carries clean B4 and C1 instances. Written by the party being evaluated (proposed 2026-08-27) |
| [`2026-08-28-the-skip-was-the-whole-story.md`](2026-08-28-the-skip-was-the-whole-story.md) | A library was declared and never installed, so eleven checks on a published result stood down for ten days — reported as `1 skipped`, exit 0, badge green. Switching the alarm on found, inside one CI run, that the published numbers reproduced only on the machine that made them. Then the first repair was wrong about the cause (*"I repeated that error by believing it"*), and a fairness assertion was found passing because it compared **seeds** instead of **recordings**. | **Beginner headline (~2 min), advanced body (~15 min setup).** **Closes the `points.md` B4 worked example**, whose prediction about the fix was wrong in the useful direction. Candidate for B4 and B2; carries clean A3 and B6 instances. Written by a session that was not there; verification appendix upgraded three rows and turned up one finding no commit reports (proposed 2026-08-28) |
| [`2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md`](2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md) | **The first native case here — it happened in this repo, on 2026-08-29.** A cross-session board built the previous day, working and selftest-green, was **blank at the moment two live sessions collided in the same files** — because the session that should have been on it released its claim and then worked for 37 more minutes. It *did* record what it was writing, into a `DONE` block's `Writes:` line, which `--list` skips: replayed, the tool prints `(no active claims)` against a board that names the file. Also: `git` cannot attribute a commit to a session, and **the investigating session grepped its own live transcript, counted mentions its own investigation had just written, and pushed a false confession to every machine.** | **Beginner headline (~2 min), short body (~10 min).** Point 4 needs zero vocabulary. **Third C3 instance and the first about two sessions *simultaneously live*** rather than about session end. Carries B4 (a tier-2 mechanism dressed as tier-4, beside a real tier-4 hook in the same `.claude/`), B7 rule 3 turning on itself, and C1 from the inside. Points 1–3 written by a non-participant; **Point 4 by the party being evaluated** |
| [`2026-08-29-the-third-attempt-introduced-the-defect.md`](2026-08-29-the-third-attempt-introduced-the-defect.md) | **The assistants were not ignoring the documentation — they were obeying it.** Twelve words in a module docstring (*"the interval between them is its duration"*) licensed a computation the project spent a month saying was not its to make, and spread to fourteen files including the public site. **The clause was added by the commit whose stated purpose was to stop that file misleading people** — *"The third attempt at a sentence two people have now been misled by"* — and corrected nothing. Also: an open todo's recommendation kept being executed; the prose was the live defect while the code defect never shipped a number; and the new prose rule already misses two strings on the deployed page. | **Beginner headline (~2 min), advanced body (~10 min setup).** **Rule 10's cost side**, which `points.md` currently states only as an upside — and a rare *counter*-instance to B4, where prose was the most effective enforcement in the project and was pointed the wrong way. Carries B7 rule 3 (a stale todo is a standing instruction), a discriminator B7 rule 8 needs (jurisdictional vs numerical bans), and a **fourth selftest instance** one day old. **Carries a bench allegory (Tony's) built to be opened with the wrong answer** — a post-it nobody read, corrected to a laminated card everybody read and obeyed — which teaches Points 1–3 with no software vocabulary. Written by a non-participant; two edits in the branch are marked as mine |
| [`2026-08-29-two-sessions-three-minutes-apart.md`](2026-08-29-two-sessions-three-minutes-apart.md) | **Second native case.** Two sessions spawned the same eleven-role review of the same page **2m51s apart**. One had posted *"are you in this file?"* on the shared board five minutes earlier and read the silence as an answer; the other's *"STOP"* arrived eight minutes after twenty-two agents were already in flight. **The board was not empty — it was staffed, accurate, and slower than the decision.** The STOP also dated the other run *"since ~15:55"* when its own transcripts start at **16:12:30**, and the author of this case repeated that figure into a handoff unchecked — the third time in one afternoon he passed on a confident number. Also: the accidental duplicate is an experiment nobody would fund, and **the two blind panels converged on the same top three defects.** | **Beginner headline (~2 min), short body (~10 min).** **Fourth C3 instance and the first where the mechanism was working correctly at the moment it failed.** Strongest B4 in the repo — nobody skipped the mechanism; the failure was underneath it. Also C1 and B2/A3. **Written by one of the two parties, who never asked the other**; the hinge fact is read in the other's favour |
