# `Mac/a52b2bae` as the session that was written over

**Status:** OPEN
**Parked:** 2026-08-29 by `Mac/9b26b5c4`

## What I actually have
`docs/cases/2026-08-29-the-board-was-empty-because-claiming-is-a-habit.md` identifies
`Mac/a52b2bae` as the session collided with at 14:33. The basis is file-access timestamps only: it
was inside `search-to-shipped.html` from 14:24:56 to 14:41:52, spanning the sweep at 14:33:38.

## Why I do not trust it
**I never read that session's complaint.** Tony reported *"another session is complaining about
files changing"*; I inferred which one. It is already marked `not confirmed` in the case's
verification appendix — this file exists so the doubt outlives the appendix.

The specific reason for caution: in that same investigation I asserted a different attribution
from a different count and **got it wrong**, publishing a false confession to the shared board.
Asserting a second attribution from a second count would be the same error in better clothes.

## What would settle it
Ask `a52b2bae`, or read its transcript around 14:33. Both cheap; neither done.

## What breaks if it is wrong
The case is wrong about **who** was collided with and right about everything else — the mechanism,
the timeline, the board being empty, and all four findings. Low blast radius, but it names a
specific session in a committed teaching document, which is worth being right about.
